"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { adminFetch } from "@/lib/adminApi";

type Dashboard={total_users:number;active_users:number;verified_users:number};
type Audit={id:string;admin_user_id:string|null;action:string;resource_type:string;resource_id:string|null;result:string;reason:string|null;created_at:string};
export default function AdminDashboard(){const[d,setD]=useState<Dashboard|null>(null),[audit,setAudit]=useState<Audit[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{(async()=>{try{const[a,b]=await Promise.all([adminFetch("/api/v1/admin/dashboard"),adminFetch("/api/v1/admin/audit-logs?limit=8")]);const ad=(await a.json()) as Dashboard&{detail?:string};const bd=(await b.json()) as Audit[]&{detail?:string};if(!a.ok)throw new Error(ad.detail||"Unable to load dashboard");if(!b.ok)throw new Error((bd as {detail?:string}).detail||"Unable to load audit activity");setD(ad);setAudit(bd)}catch(e){setError(e instanceof Error?e.message:"Unable to load dashboard")}finally{setLoading(false)}})()},[]);return <div className="mx-auto max-w-7xl space-y-7"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm text-slate-400">Admin operations</p><h2 className="mt-1 text-2xl font-bold tracking-tight">Exchange overview</h2><p className="mt-2 text-sm text-slate-500">Live operational metrics from the admin APIs.</p></div><Link href="/admin/kyc" className="rounded-xl bg-cyan-500 px-4 py-2.5 text-sm font-bold text-slate-950">Review KYC queue</Link></div>{error&&<div className="rounded-xl border border-red-500/20 bg-red-500/5 p-3 text-sm text-red-300">{error}</div>}<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Total users",d?.total_users??"—","text-cyan-300"],["Active users",d?.active_users??"—","text-emerald-300"],["Verified users",d?.verified_users??"—","text-violet-300"],["Pending operations","See queues","text-amber-300"]].map(([label,value,tone])=><div key={label} className="rounded-2xl border border-slate-800 bg-[#0d1422] p-5"><div className="text-sm text-slate-500">{label}</div><div className={`mt-3 text-2xl font-bold ${tone}`}>{loading?"…":value}</div><div className="mt-2 text-xs text-slate-500">Live API data</div></div>)}</div><div className="grid gap-6 xl:grid-cols-[1.5fr_1fr]"><div className="rounded-2xl border border-slate-800 bg-[#0d1422] p-5"><div className="flex items-center justify-between"><div><h3 className="font-semibold">Recent admin activity</h3><p className="mt-1 text-xs text-slate-500">Audit events from the backend</p></div><Link href="/admin/audit" className="text-xs font-semibold text-cyan-400">View all →</Link></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b border-slate-800 text-xs uppercase tracking-wider text-slate-500"><tr><th className="pb-3">Action</th><th className="pb-3">Resource</th><th className="pb-3">Result</th><th className="pb-3">Time</th></tr></thead><tbody>{loading?<tr><td colSpan={4} className="py-10 text-center text-slate-500">Loading activity...</td></tr>:audit.length===0?<tr><td colSpan={4} className="py-10 text-center text-slate-500">No audit activity yet.</td></tr>:audit.map(x=><tr key={x.id} className="border-b border-slate-800/70 last:border-0"><td className="py-4 font-medium text-slate-200">{x.action}</td><td className="py-4 text-slate-400">{x.resource_type}{x.resource_id?` · ${x.resource_id.slice(0,8)}`:""}</td><td className="py-4 text-emerald-300">{x.result}</td><td className="py-4 text-slate-500">{new Date(x.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></div><div className="rounded-2xl border border-slate-800 bg-[#0d1422] p-5"><h3 className="font-semibold">Operations</h3><p className="mt-1 text-xs text-slate-500">Quick access to live queues</p><div className="mt-5 space-y-3">{[["Users","/admin/users"],["KYC","/admin/kyc"],["Deposits","/admin/deposits"],["Withdrawals","/admin/withdrawals"]].map(([label,href])=><Link key={label} href={href} className="flex items-center justify-between rounded-xl border border-slate-800 bg-slate-900/50 p-4 hover:border-slate-700"><span className="text-sm font-medium">{label}</span><span className="text-slate-500">→</span></Link>)}</div></div></div></div>}
